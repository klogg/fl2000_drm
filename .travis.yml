
language: c

os:
   -linux

compiler: gcc

addons:
   apt:
      sources:
         - ubuntu-toolchain-r-test
      packages:
         - build-essential
         - kernel-headers-$(uname -r)

dist: bionic

script:
   - make
